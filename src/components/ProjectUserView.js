import React from "react";
import PropTypes from 'prop-types';

export default function ProjectUserView() {
    return (
        <React.Fragment>
         <h4>Project for User</h4>
        </React.Fragment>
    );
}